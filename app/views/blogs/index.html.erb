<h1>つぶやき一覧</h1>

<table>
    <tr>あなたのつぶやき一覧</tr>
    
<% @blogs.each do |blog| %>
    <tr>
        <td><%= blog.content %></td>
        <td><%= link_to '詳細を確認', blog_path(blog.id) %></td>
        <td><%= link_to 'つぶやきを編集', edit_blog_path(blog.id) %></td>
        <td><%= link_to 'つぶやきを削除', blog_path(blog.id), method: :delete, data: { confirm: '本当に削除していいですか？' } %></td>
    </tr>
<% end %>
</table>

<%= link_to '新しくつぶやきを投稿する', new_blog_path %>