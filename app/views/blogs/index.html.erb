<h1>Tweet一覧</h1>

<table>
    <tr>つぶやき</tr>
    
<% @blogs.each do |blog| %>
    <tr>
        <td><%= blog.content %></td>
        <td><%= link_to '詳細を確認する', blog_path(blog.id) %></td>
        <td><%= link_to "ブログを編集する", edit_blog_path(blog.id) %></td>
    </tr>
<% end %>
</table>

<%= link_to '新しくつぶやきを投稿する', new_blog_path %>